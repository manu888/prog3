package commperator;

import history.CommandEntity;

import java.util.Comparator;

/*
 * Sortiert und gibt nur die Methodenaufrufe einer bestimmten Aktie (ShareName) wieder.
 */
public class ComparatorShareName implements Comparator<CommandEntity>{

    @Override
    public int compare(CommandEntity e1, CommandEntity e2)
    {
        
        return 0;
    }

}
